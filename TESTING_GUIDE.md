# Руководство по тестированию исправлений

## Что было исправлено:

### 1. Отправка фотографий и файлов
- Исправлена логика загрузки файлов
- Теперь файл сначала загружается на сервер, затем создается сообщение с вложением
- Backend обновлен для поддержки вложений при создании сообщения

### 2. Эмодзи
- Эмодзи пикер для ввода сообщений работает (кнопка 😊)
- Эмодзи реакции на сообщения работают (кнопка 😊 в футере сообщения)
- Исправлено закрытие эмодзи пикера при клике вне его

### 3. Голосовые сообщения
- Функция записи голоса работает (кнопка 🎤)
- Удерживайте кнопку для записи
- Отпустите для отправки
- Голосовое сообщение загружается как аудио файл

## Как запустить для тестирования:

### Backend:
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend:
```bash
cd frontend
npm run dev
```

## Как протестировать:

1. **Фотографии**: 
   - Нажмите кнопку 🖼
   - Выберите изображение
   - Проверьте, что оно отображается в чате

2. **Эмодзи в сообщениях**:
   - Нажмите кнопку 😊 в панели ввода
   - Выберите эмодзи из списка
   - Эмодзи добавится в текст сообщения

3. **Реакции на сообщения**:
   - Нажмите кнопку 😊 в футере любого сообщения
   - Выберите эмодзи реакцию
   - Реакция добавится к сообщению

4. **Голосовые сообщения**:
   - Зажмите кнопку 🎤
   - Говорите в микрофон (браузер запросит разрешение)
   - Отпустите кнопку
   - Голосовое сообщение отправится

## Технические детали изменений:

### Frontend (ChatDetailView.vue):
- Обновлена функция `uploadFile()` для передачи данных о файле в сообщение
- Исправлена функция `handleClickOutside()` для корректного закрытия эмодзи пикеров
- Удалены неиспользуемые функции `toggleReaction()` и `groupReactions()`

### Backend:
- `schemas/message.py`: Добавлена схема `MessageAttachmentCreate`
- `api/messages.py`: Обновлена функция `create_message()` для создания вложений
