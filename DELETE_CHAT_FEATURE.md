# ✅ Удаление чата

## 🗑️ Новая функция

Добавлена возможность полностью удалить чат со всеми сообщениями.

## 🔧 Реализация

### Frontend

**Кнопка удаления в меню чата:**
```vue
<MenuItem v-slot="{ active }">
  <button
    @click="deleteChat"
    :class="[
      active ? 'bg-red-50 text-red-600' : 'text-red-600',
      'group flex items-center w-full px-3 py-2 text-sm rounded-md font-semibold'
    ]"
  >
    <TrashIcon class="w-5 h-5 mr-2" />
    Удалить чат
  </button>
</MenuItem>
```

**Функция удаления:**
```javascript
async function deleteChat() {
  if (!confirm('Вы уверены, что хотите удалить этот чат? Все сообщения будут удалены безвозвратно.')) {
    return
  }
  
  try {
    await api.delete(`/chats/${route.params.id}`)
    alert('Чат удален')
    router.push('/chats')
  } catch (error) {
    console.error('Delete chat error:', error)
    const errorMessage = error.response?.data?.detail || error.message
    alert('Ошибка удаления чата: ' + errorMessage)
  }
}
```

### Backend

**Endpoint уже существует:**
```python
@router.delete("/{chat_id}")
async def delete_chat(
    chat_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    result = await db.execute(
        select(Chat)
        .options(selectinload(Chat.members))
        .where(Chat.id == chat_id)
    )
    chat = result.scalar_one_or_none()
    
    if not chat:
        raise HTTPException(status_code=404, detail="Chat not found")
    
    # Только владелец может удалить чат
    member = next((m for m in chat.members if m.user_id == current_user.id), None)
    if not member or member.role != ChatMemberRole.OWNER:
        raise HTTPException(status_code=403, detail="Only owner can delete chat")
    
    await db.delete(chat)
    await db.commit()
    
    return {"message": "Chat deleted successfully"}
```

## 🔒 Безопасность

### Проверка прав:
- ✅ Только владелец чата может удалить его
- ✅ Участники не могут удалить чат
- ✅ Проверка на существование чата

### Подтверждение:
- ✅ Двойное подтверждение (confirm)
- ✅ Предупреждение о безвозвратном удалении
- ✅ Детальные сообщения об ошибках

## 📊 Что удаляется

При удалении чата удаляются:
- ✅ Сам чат
- ✅ Все сообщения (CASCADE)
- ✅ Все вложения (CASCADE)
- ✅ Все реакции (CASCADE)
- ✅ Все связи с участниками (CASCADE)

## 🎨 UI/UX

### Визуальное оформление:
- **Очистить историю** - оранжевый цвет (предупреждение)
- **Удалить чат** - красный цвет (опасность)
- **Удалить чат** - жирный шрифт (важность)

### Расположение:
```
Меню чата (⋮)
├── Переименовать
├── Информация
├── Очистить историю (оранжевый)
└── Удалить чат (красный, жирный)
```

## 🧪 Тестирование

### Тест 1: Удаление как владелец

**Шаги:**
1. Откройте чат, который вы создали
2. Нажмите меню (⋮)
3. Выберите "Удалить чат"
4. Подтвердите удаление

**Ожидаемый результат:**
- ✅ Появляется подтверждение
- ✅ Чат удаляется
- ✅ Переход на страницу чатов
- ✅ Чат исчезает из списка

---

### Тест 2: Попытка удаления как участник

**Шаги:**
1. Откройте чат, где вы не владелец
2. Нажмите меню (⋮)
3. Выберите "Удалить чат"
4. Подтвердите удаление

**Ожидаемый результат:**
- ✅ Ошибка: "Only owner can delete chat"
- ✅ Чат не удаляется

---

### Тест 3: Отмена удаления

**Шаги:**
1. Откройте чат
2. Нажмите меню (⋮)
3. Выберите "Удалить чат"
4. Нажмите "Отмена" в подтверждении

**Ожидаемый результат:**
- ✅ Чат не удаляется
- ✅ Остаетесь в чате

---

### Тест 4: Проверка каскадного удаления

**Шаги:**
1. Создайте чат
2. Отправьте несколько сообщений
3. Добавьте файлы
4. Удалите чат
5. Проверьте базу данных

**Ожидаемый результат:**
- ✅ Чат удален
- ✅ Сообщения удалены
- ✅ Вложения удалены
- ✅ Связи удалены

## 🔄 Сравнение функций

| Функция | Что удаляет | Кто может | Восстановление |
|---------|-------------|-----------|----------------|
| **Очистить историю** | Только сообщения | Любой участник | Невозможно |
| **Удалить чат** | Чат + все данные | Только владелец | Невозможно |

## 💡 Рекомендации

### Когда использовать "Очистить историю":
- Нужно удалить старые сообщения
- Хотите начать с чистого листа
- Чат нужен, но история не важна

### Когда использовать "Удалить чат":
- Чат больше не нужен
- Хотите полностью удалить все данные
- Вы владелец и уверены в решении

## ⚠️ Предупреждения

### Для пользователей:
- ⚠️ Удаление необратимо
- ⚠️ Все сообщения будут потеряны
- ⚠️ Все файлы будут удалены
- ⚠️ Восстановление невозможно

### Для владельцев:
- ⚠️ Удаление чата удалит его для всех участников
- ⚠️ Участники потеряют доступ к истории
- ⚠️ Убедитесь, что важные данные сохранены

## 🎯 Сценарии использования

### Сценарий 1: Завершенный проект
1. Проект завершен
2. Чат больше не нужен
3. Владелец удаляет чат
4. Все данные удалены

### Сценарий 2: Ошибочно созданный чат
1. Создали чат по ошибке
2. Сразу удаляют
3. Чат исчезает

### Сценарий 3: Очистка старых чатов
1. Много неактивных чатов
2. Владелец удаляет ненужные
3. Список чатов чище

## 🚀 Frontend обновлен

Функция удаления чата добавлена!

**Попробуйте сейчас:**
1. Откройте чат
2. Меню (⋮) → Удалить чат
3. Подтвердите удаление

Чат будет удален! 🗑️
