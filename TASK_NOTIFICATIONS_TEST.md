# 🧪 Тестирование уведомлений о задачах

## Быстрый тест

### 1. Подготовка
```bash
# Убедитесь, что бэкенд и фронтенд запущены
# Бэкенд: http://localhost:8000
# Фронтенд: http://localhost:5173
```

### 2. Тест уведомлений

**Шаг 1:** Откройте два браузера (или два окна в режиме инкогнито)
- Браузер A: войдите как Пользователь 1
- Браузер B: войдите как Пользователь 2

**Шаг 2:** В Браузере B разрешите уведомления (если появится запрос)

**Шаг 3:** В Браузере A:
- Перейдите в раздел "Задачи"
- Нажмите "Новая задача"
- Заполните форму и назначьте задачу Пользователю 2
- Сохраните

**Ожидаемый результат в Браузере B:**
- ✅ Появится браузерное уведомление "Новая задача: [название]"
- ✅ Прозвучит звуковой сигнал
- ✅ Задача автоматически появится в списке
- ✅ Счетчик "Назначенные мне" увеличится

### 3. Тест фильтрации

**Шаг 1:** Создайте несколько задач:
- 2 задачи, назначенные вам
- 2 задачи, назначенные другим
- 2 задачи без назначения

**Шаг 2:** Проверьте фильтры:
- Нажмите "Все" → должны показаться все 6 задач
- Нажмите "Назначенные мне" → должны показаться только 2 задачи
- Нажмите "Созданные мной" → должны показаться все созданные вами задачи

**Шаг 3:** Обновите страницу
- ✅ Выбранный фильтр должен сохраниться

### 4. Тест сортировки

**Шаг 1:** Создайте задачи с разными параметрами:
- Задача с приоритетом "Срочный"
- Задача с приоритетом "Низкий"
- Задача с ближайшим дедлайном
- Задача с дальним дедлайном

**Шаг 2:** Переключайте сортировку:
- "По приоритету" → срочные должны быть сверху
- "По дедлайну" → ближайшие дедлайны сверху
- "По дате создания" → новые задачи сверху

**Шаг 3:** Обновите страницу
- ✅ Выбранная сортировка должна сохраниться

## Проверка WebSocket

Откройте консоль браузера (F12) и проверьте:

```javascript
// Должны видеть сообщения типа:
WebSocket connection established
Task assigned: { task_id: 123, task_title: "...", ... }
```

## Возможные проблемы

### Уведомления не приходят
1. Проверьте, что WebSocket подключен (в консоли должно быть "WebSocket connection established")
2. Проверьте, что разрешены браузерные уведомления
3. Проверьте, что бэкенд запущен и доступен

### Звук не воспроизводится
1. Проверьте, что файлы звуков есть в `frontend/public/sounds/`
2. Проверьте настройки звука в профиле пользователя
3. Проверьте, что браузер не заблокировал автовоспроизведение

### Фильтры не работают
1. Проверьте, что в ответе API есть поле `assignees`
2. Откройте Network tab и проверьте ответ от `/api/tasks`
3. Проверьте консоль на наличие ошибок

## API Endpoints для ручного тестирования

### Создать задачу с назначением
```bash
curl -X POST http://localhost:8000/api/tasks \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовая задача",
    "description": "Описание",
    "priority": "high",
    "assignee_ids": [2, 3]
  }'
```

### Получить список задач
```bash
curl http://localhost:8000/api/tasks \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Успешный тест ✅

Если все работает, вы должны увидеть:
- ✅ Уведомления приходят в реальном времени
- ✅ Звук воспроизводится
- ✅ Фильтры работают корректно
- ✅ Сортировка работает
- ✅ Настройки сохраняются после перезагрузки
- ✅ Счетчики обновляются автоматически

Готово! 🎉
